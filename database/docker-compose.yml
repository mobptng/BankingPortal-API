services:
  mysql:
    image: mysql:lts
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - "./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql"